import { CollaborationList } from "@/components/shared/collaboration-list";

export const metadata = { title: "Együttműködések" };

export default function BrandCollaborationsPage() {
  return <CollaborationList />;
}
